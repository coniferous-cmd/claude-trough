# Product Requirements

## Vision

A terminal-first todo manager — `trough`.

## Core Features

- Command mode
- List mode
- SQLite storage (`~/.config/trough/trough.db`)
- Vim detail editing
- Single binary
